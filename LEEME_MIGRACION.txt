CÓMO MIGRAR ESTOS CAMBIOS A TU REPOSITORIO — RONDA 3
======================================================
(H7: API REST · H10: tests de humo · H8: cierre de la 3ra duplicación
de agregación + decisión de peso granular)

PASO 1 — Copiar los archivos nuevos/modificados
------------------------------------------------
Arrastra TODO el contenido de este zip (.gitattributes, .gitignore,
README.md, api/, app/, requirements.txt, spatial/, tests/) encima de
la carpeta de tu repositorio real, dejando que se sobrescriban los
archivos que ya existían.

Si NO habías migrado la ronda anterior (API/tests/smoke tests) por
separado, este zip ya la incluye completa — no hace falta que apliques
las dos rondas por separado, esta contiene todo.

PASO 2 — Instalar Flask (si no lo hiciste en la ronda anterior)
------------------------------------------------
    pip install -r requirements.txt

PASO 3 — Verificar que todo sigue funcionando
------------------------------------------------
    python -m pytest tests/ -q

Debe decir "348 passed" al final.

PASO 4 — Qué cambió de verdad en esta ronda (para tu conocimiento)
------------------------------------------------
- Se agregó un campo NUEVO en el motor (spatial/decision_support/):
  `CommunityProfile.peso_granular` / `participacion_pct_granular`.
  El campo existente `peso_total` NO se tocó — sigue calculando
  exactamente lo mismo que antes.
- `app/pages/4_Spatial_Cluster_Intelligence.py` (Cluster Intelligence)
  ahora usa ese campo nuevo para mostrar "peso económico de comunidad"
  en vez de su cálculo local anterior. Esto SÍ cambia los números que
  se muestran en esa página (ver la auditoría para el detalle:
  "peso económico" ahora es ~2.86x mayor en total y se redistribuye
  distinto entre comunidades — es la corrección de un error real, no
  un cambio cosmético).
- Se agregó `api/` — un servidor REST nuevo (Flask). No se activa solo;
  para correrlo hay que ejecutar `python -m api.app` a propósito.

NOTA: NO toques nada dentro de tu carpeta data/ — esos son tus
artefactos reales, no se tocaron en esta corrección.
